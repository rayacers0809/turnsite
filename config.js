/* ============================================================
   TURN CITY — 사이트 설정
   백엔드(Railway)를 쓰려면 아래에 API 주소를 넣으세요.
   비워두면 로컬 data.js 를 사용합니다.
   예: window.TURN_API_BASE = "https://turncity-api.up.railway.app";
   ============================================================ */
window.TURN_API_BASE = "";
